# 餐饮点餐系统 · 项目情况说明

> 更新时间：2026-08-06
> 仓库地址：https://github.com/qiaosong99/foot

## 一、项目概述

基于**局域网**的扫码点餐系统，无需外网即可运行。一台 Windows 电脑（**支持 Windows 7**）作为服务器（桌面端程序内嵌后端），店内各终端通过局域网 WiFi 连接，覆盖顾客扫码点餐、后厨接单、收银结算、服务员代点、小票打印全流程。

## 二、系统架构（五端）

| 端 | 目录 | 技术栈 | 说明 |
|---|---|---|---|
| 后端服务 | `server/` | Node.js + Express + **better-sqlite3** + Socket.io | 单端口 3000，托管 API + 静态页面 |
| 管理后台（Web） | `admin/` | Vue 3 + Vite + Element Plus | 菜品/订单/桌台/会员/优惠券/装修 |
| 顾客端 + 后厨端（Web） | `web/` | Vue 3 + Vite + Vant + Socket.io | 扫码点餐、订单状态、后厨看板 |
| 桌面端 | `desktop/` | Electron 22 + Vue 3 + Element Plus | 大屏/后厨/收银/退菜/外卖/管理/设置，**内嵌后端服务** |
| 服务员端（App） | `waiter-app/` | uni-app + Capacitor 8 + Android | 桌台/代客点单/退菜/服务器配置 |

### Win7 兼容架构（重点）

- **后端运行在 Electron 主进程内**（不再外置 node.exe）：Electron 22 支持 Win7，规避了 Node 14+ 依赖 Win8 API（GetSystemTimePreciseAsFileTime）的问题
- **数据层 Prisma → better-sqlite3**：自实现 Prisma 兼容适配层（`server/src/services/db.js`），无 Rust 引擎依赖；直接复用原 Prisma 生成的 dev.db（DateTime 存毫秒时间戳），**零数据迁移**
- 打包内的 better-sqlite3 使用 Electron ABI 预编译（v9.x，打包目录 `desktop/build/server`）；开发环境用 v13（系统 Node）

### 桌面端说明（核心交付物）

- 打包后后端随主进程自动启动（进程内模式），开机即用
- 双通道打印：
  - **系统打印机模式**：Electron 静默打印 HTML 小票，支持任意已安装驱动的打印机（USB/共享）
  - **网络打印机模式**：ESC/POS 指令 + TCP 9100 端口
- 打印链路：测试打印与正式打印（收银/外卖）走**同一入口函数**，保证测试通过即可实际出纸

## 三、功能清单

### 顾客端（手机扫码）
- 分类浏览、购物车、规格选择、下单
- 订单状态实时跟踪、请求结账

### 后厨端（Web 看板）
- 新订单实时推送（Socket）、声音提醒
- 开始制作、单品上菜、整单上菜、退菜联动

### 收银/结算
- 待结算列表、确认结算
- 结算后询问是否打印小票（订单信息套入模板真实打印）
- 今日收入统计与分时柱状图
- **历史订单**：日期范围/状态筛选 + 分页，可查看每单顾客点了什么（详情弹窗）
- **周/月报表**：每日营业额与订单数、菜品销量 TOP10、周期导航，支持**导出 CSV**（Excel 直接打开）

### 大屏（桌台总览）
- 顶部实时时钟 + 桌台搜索
- 四态卡片：空台/占用/预订/预结，带筛选标签与分页
- 右侧详情面板：开台时间/服务类型/订单明细/优惠小计合计服务费汇总、整桌结算
- **桌台预订**：大屏可标记/取消预订（status=2）

### 退菜
- 桌面端独立"退菜"模块（选桌台 → 当前点单 → 逐菜退）
- 服务员 App 同样支持，两端实时同步
- 退完自动取消订单、释放桌台；已结算订单不可退

### 桌台管理
- 桌台增删改、二维码生成/下载/一键全部刷新
- **强制释放**：0 元订单等无法结算场景可强制取消订单并释放桌台

### 外卖
- 桌面端外卖开单（选外卖桌台、购物车、提交）
- 提交后询问是否打印外卖小票

### 打印与小票
- 打印机设置：双模式切换、自动枚举 Windows 已安装打印机、局域网扫描
- 小票模板：店名/电话/地址/头尾文字/纸宽（58/80mm），所见即所得预览
- 测试打印：使用当前模板 + 示例订单，走正式打印相同链路

### 其他
- 设置页显示本机局域网 IP（`http://IP:3000` 格式、一键复制），多 IP 时标注**推荐**（默认网关所在网卡）并给出选择指引，方便手机配置
- 服务员 App 内置网络诊断（网络类型/局域网探测/外网对照）

## 四、构建产物

| 产物 | 路径 |
|---|---|
| 桌面端安装包 | `desktop/release/餐饮点餐系统 Setup 1.0.0.exe`（约 146MB） |
| 桌面端绿色版 | `desktop/release/win-unpacked/餐饮点餐系统.exe` |
| 服务员 APK | `服务员端.apk`（项目根目录，源：`waiter-app/android/app/build/outputs/apk/debug/app-debug.apk`） |

### 构建命令（PowerShell 需用 npm.cmd）

```powershell
# 管理后台 / 顾客端
npm.cmd run build --prefix d:\foot\admin
npm.cmd run build --prefix d:\foot\web

# 桌面端（vite + electron-builder，产物在 desktop/release）
npm.cmd run build:win --prefix d:\foot\desktop

# 服务员 APK
Set-Location d:\foot\waiter-app
npm.cmd run build:h5
npx.cmd cap sync android
Set-Location android; .\gradlew.bat assembleDebug
```

## 五、启动与访问

- 启动：运行桌面端程序（自动拉起内嵌后端，约 30 秒完成初始化）；开发模式用 `start.bat`
- 管理后台账号：`admin / admin123`

| 页面 | 地址（IP 以实际为准） |
|---|---|
| 管理后台 | `http://IP:3000/admin` |
| 后厨看板 | `http://IP:3000/kitchen` |
| 后厨历史 | `http://IP:3000/kitchen/history` |
| 顾客点餐 | `http://IP:3000/c/menu?table=A01` |
| 服务员 App | 设置中填 `http://IP:3000` |

## 六、部署要点（踩坑记录）

1. **Windows 防火墙**：公用网络下需添加入站规则（TCP 3000），否则手机无法访问
2. **服务员 App 网络**：Capacitor 默认 `https://localhost` 会拦截 http 请求（混合内容），已配置 `androidScheme: http` 解决
3. **手机热点拓扑**：手机开热点 + 电脑连热点时，部分手机 App 访问热点客户端不稳定，建议用路由器或"电脑开热点 + 手机连电脑"
4. **打印机**：系统打印机模式需已安装驱动；网络打印机模式需开启 9100 端口
5. **打包前**：若改动后端，需同步 `server/src` 到 `desktop/build/server/src`；数据库文件位于 `build/server/prisma/dev.db`
6. **Win7 支持**：不得在打包目录重新引入外置 node.exe（Node 14+ 无法在 Win7 启动）；better-sqlite3 需用 Electron ABI 预编译（打包目录内为 v9.x）
7. **多 IP 场景**：多网卡/热点机器会出现多个 IP，服务监听 0.0.0.0 都可用，手机需选与其同网段的那个（设置页有推荐标识）

## 七、后续规划：SaaS 化

1. 数据库 SQLite → MySQL/PostgreSQL，全表增加 `tenantId` 多租户隔离
2. 云端部署 + HTTPS + 严格 JWT 鉴权
3. 商户体系（注册/开通/角色权限）+ 计费订阅
4. 顾客端微信小程序化 + 微信/支付宝支付
5. 云打印（飞鹅/易联云）+ 断网容灾混合模式
