# 项目交接文档（面向 AI 助手）

> 最后更新：2026-08-06
> 仓库：https://github.com/qiaosong99/foot
> 本文档供接手的 AI 助手快速理解项目全貌、关键架构约束与踩坑记录。

---

## 1. 项目是什么

**局域网扫码点餐系统**：无需外网，一台 Windows 电脑（**必须兼容 Windows 7**）作为服务器，店内各终端通过局域网 WiFi 连接。五端架构：

| 端 | 目录 | 技术栈 | 形态 |
|---|---|---|---|
| 后端 | `server/` | Node.js + Express + **better-sqlite3** + Socket.io | 内嵌于桌面端进程内运行，单端口 3000 |
| 管理后台 | `admin/` | Vue3 + Vite + Element Plus | 静态页面，后端托管于 `/admin` |
| 顾客端+后厨端 | `web/` | Vue3 + Vite + Vant | 静态页面，`/c/*` 和 `/kitchen` |
| 桌面端 | `desktop/` | Electron 22 + Vue3 + Element Plus | **核心交付物**，exe 安装包 |
| 服务员端 | `waiter-app/` | uni-app + Capacitor 8 | Android APK |

交付物（均在项目根目录）：
- `餐饮点餐系统 Setup 1.0.0.exe`（约 93MB，NSIS 安装包）
- `服务员端.apk`
- 源：`desktop/release/`、`waiter-app/android/app/build/outputs/apk/debug/`

---

## 2. 不可违反的架构约束（重要！）

这些是踩坑后确定的设计，改动前务必理解原因：

### 2.1 Win7 兼容（最高优先级约束）
- 客户机器是 Windows 7。**Node.js 14+ 依赖 Win8 才有的 `GetSystemTimePreciseAsFileTime` API，在 Win7 无法启动**。
- 因此：**打包产物中绝对不能出现外置 node.exe**。后端通过 `require(resources/server/src/app.js)` 运行在 **Electron 22 主进程内**（Electron 22 是最后一个支持 Win7 的版本，不要升级 Electron）。
- 开发模式（非打包）仍用系统 node 启动，见 `desktop/main.js` 的 `startServer()`。

### 2.2 数据层是 better-sqlite3，不是 Prisma（运行时）
- 历史原因项目里有 `prisma/schema.prisma`，**它只是表结构文档，运行时不使用 Prisma**。
- 实际数据层：`server/src/services/db.js` —— 自实现的 **Prisma 兼容适配层**（findUnique/findMany/create/update/updateMany/upsert/delete/count/aggregate/include/_count），底层 better-sqlite3。路由文件 `require('../services/prisma')` 拿到的就是这个适配层（`services/prisma.js` 只是转发）。
- 数据库文件：`prisma/dev.db`（标准 SQLite，**DateTime 存毫秒时间戳**，读取时适配层转 ISO 字符串输出）。
- 关键实现细节（改 db.js 前必读）：
  - `Order` 是 SQLite 保留字，表名需 `"Order"` 引号（`TABLE()` 函数）
  - `values`、`key` 是保留字，所有列名用 `COL()` 加引号
  - `include` 关联组装：many 用 `WHERE fk IN (...)` 批量查后分组，one 用外键映射
  - 嵌套 where（如 `where.table.tableNo.contains`）转 EXISTS 子查询
  - `updateMany` 用 `UPDATE t AS t` 别名语法
- **两个版本的 better-sqlite3**：
  - 开发环境 `server/node_modules`：v13（配系统 Node 20+）
  - 打包目录 `desktop/build/server/node_modules`：**v9.x + Electron ABI 预编译**（v13 要求 Node22+，Electron 22 内加载会崩）
  - 给打包目录装包时必须带 `$env:npm_config_runtime='electron'; $env:npm_config_target='22.3.27'`

### 2.3 打印架构（双通道 + 链路一致原则）
- 桌面端打印走 IPC（`desktop/main.js` → `desktop/print/index.js`）：
  - **系统打印机模式**（printMode=system）：HTML 小票 + Electron 静默打印（`webContents.print`，按内容高度算纸张长度），支持任意已装驱动的打印机
  - **网络打印机模式**（printMode=network）：ESC/POS + TCP 9100
- **测试打印与正式打印（收银/外卖）必须走同一入口 `doPrint()`**，测试用当前模板+示例订单。不要给测试打印单独开链路。

### 2.4 服务地址与二维码联动
- 桌面端"设置→服务器"保存的地址：写 localStorage + userData 的 `server-config.json`（双保险）+ 同步后端 Setting 表 `server_address`。
- **二维码生成（单个/一键刷新）优先使用 `server_address`**（`admin.js` 的 `getQrServerIP()`），未设置才自动检测网卡。
- 二维码图片路径必须带 `?v=时间戳` 防缓存。

### 2.5 服务员 App
- Capacitor 必须保持 `"server": { "androidScheme": "http" }`（`waiter-app/capacitor.config.json`），否则 WebView 混合内容拦截所有 http 请求（表现为 request:fail 1-5ms 瞬间失败）。

---

## 3. 目录与关键文件

```
d:\foot\
├── server/                    # 后端
│   ├── src/app.js             # Express 入口（require 即 listen，export {app, server}）
│   ├── src/services/db.js     # ★ better-sqlite3 Prisma兼容适配层（核心）
│   ├── src/services/prisma.js # 转发 db.js（保持路由 require 路径不变）
│   ├── src/routes/            # admin(最大)/customer/kitchen/waiter/printer/upload
│   ├── src/socket/index.js    # Socket.io 事件广播
│   ├── src/middleware/auth.js # 局域网模式：无 token 也放行（有意为之）
│   └── prisma/dev.db          # SQLite 数据库文件（schema.prisma 仅为文档）
├── desktop/                   # Electron 桌面端
│   ├── main.js                # ★ 主进程：进程内后端/打印IPC/save-file/服务配置持久化/get-lan-ip
│   ├── preload.js             # electronAPI 暴露清单
│   ├── print/index.js         # ESC/POS 生成 + HTML 小票渲染
│   ├── src/views/             # screen(大屏)/cashier(收银含报表)/refund(退菜)/takeout/kitchen/admin/settings
│   ├── src/api/socket.ts      # socket 自动重连（地址变更自动重建）
│   ├── build/server/          # ★ 打包用后端副本（node_modules 为 Electron ABI）
│   └── release/               # 打包产物（win-unpacked + Setup.exe）
├── admin/                     # 管理后台 web
├── web/                       # 顾客+后厨 web
├── waiter-app/                # 服务员 uni-app（android/ 为 Capacitor 工程）
├── 项目情况.md                # 面向人的项目说明
└── 项目交接.md                # 本文档
```

---

## 4. 主要 API（前缀 `/api`）

- `POST /admin/login`（admin/admin123），其余 admin 接口走 authMiddleware（局域网模式实际不拦截）
- 菜品/分类/桌台/订单/会员/优惠券/广告/设置/装修：标准 CRUD，见 `routes/admin.js`
- `PUT /admin/tables/:id/reserve` 预订切换（status 0↔2）
- `PUT /admin/tables/:id/force-release` 强制释放（0元订单场景）
- `PUT /admin/tables/:id/settle-all` 整桌结算
- `GET /admin/orders?status=&settleStatus=&startDate=&endDate=&page=&pageSize=`（含 items/table）
- `GET /admin/reports/summary?period=week|month&anchor=YYYY-MM-DD` 报表（每日明细+TOP10）
- `GET /admin/tables/:id/qrcode`、`PUT /admin/tables/qrcode/refresh-all`（地址取 server_address）
- `GET /admin/screen/tables` 大屏数据（含未结算订单聚合）
- `DELETE /waiter/orders/:id/items/:itemId` 退菜
- `POST /customer/orders`、`POST /customer/orders/request-settle`
- Socket 事件：new_order / order_status_change / settle_request / settle_complete / table_status_change / dish_removed / waiter_order（房间：screen/kitchen/waiter/table_桌号）

---

## 5. 构建与打包（PowerShell 环境）

**注意**：本机 PowerShell 执行策略限制，**npm 必须用 `npm.cmd`**。

```powershell
# 后端代码变更后同步到打包目录
Copy-Item -Path d:\foot\server\src\* -Destination d:\foot\desktop\build\server\src -Recurse -Force

# 桌面端完整打包（vite + electron-builder，产物在 desktop/release）
npm.cmd run build:win --prefix d:\foot\desktop

# 管理后台/顾客端（改动了才需要）
npm.cmd run build --prefix d:\foot\admin
npm.cmd run build --prefix d:\foot\web

# 服务员 APK
Set-Location d:\foot\waiter-app; npm.cmd run build:h5; npx.cmd cap sync android
Set-Location android; .\gradlew.bat assembleDebug
Copy-Item 'app\build\outputs\apk\debug\app-debug.apk' 'D:\foot\服务员端.apk' -Force

# 打包完成后记得把安装包复制到根目录（用户要求交付物放最外层）
Copy-Item 'd:\foot\desktop\release\餐饮点餐系统 Setup 1.0.0.exe' 'd:\foot\' -Force
```

打包注意：
- 打包前**先关闭运行中的 win-unpacked 程序**（文件占用会导致打包失败/中断）
- 内嵌服务启动约需 20-30 秒初始化，验证时别太早下结论
- `build/server/` 中不要引入 node.exe、不要装 Prisma

---

## 6. 验证方法

```powershell
# 启动打包版
Start-Process "d:\foot\desktop\release\win-unpacked\餐饮点餐系统.exe"
# 等待 30 秒后
Invoke-WebRequest http://localhost:3000/admin -UseBasicParsing   # 200 即正常
# 登录
Invoke-RestMethod -Uri 'http://localhost:3000/api/admin/login' -Method Post -ContentType 'application/json' -Body '{"username":"admin","password":"admin123"}'
```

本机局域网 IP 会变化（当前曾为 10.51.49.118 / 192.168.x.x），测试手机连通性前先用 `Get-NetIPAddress -AddressFamily IPv4` 确认。

---

## 7. 已知坑清单（血泪记录）

1. **Win7 + Node14+**：弹"无法定位程序输入点 GetSystemTimePreciseAsFileTime"→ 系统不兼容，无解，只能进程内 Electron 方案
2. **防火墙**：公用网络下手机连不上 → 管理员权限加入站规则 TCP 3000（`New-NetFirewallRule -Direction Inbound -Protocol TCP -LocalPort 3000 -Action Allow -Profile Any`）
3. **Capacitor 混合内容**：App 请求 1-5ms 内全失败但浏览器正常 → 检查 androidScheme: http
4. **多网卡多 IP**：服务监听 0.0.0.0 全通，但要引导用户选与手机同网段的 IP（设置页有"推荐"标识=默认网关网卡）
5. **二维码缓存**：图片路径必须带 ?v=时间戳
6. **better-sqlite3 版本**：打包目录 v9+Electron 预编译；v13 在 Electron 22 里 napi 崩溃
7. **SQLite 保留字**：Order/values/key 需引号（db.js 已处理，新代码别绕过）
8. **DateTime 存毫秒**：直接写 SQL 时别用 ISO 字符串比较
9. **git index.lock 残留**：确认无 git 进程后直接删
10. **aapt 解析 APK**：路径不能含中文
11. **npm 在 PowerShell**：用 npm.cmd；打包时 npm 的 allowScripts 可能拦原生模块安装脚本，注意检查 .node 文件是否真实存在
12. **手机热点拓扑**：手机开热点+电脑连热点时 App 访问不稳定，建议路由器或电脑开热点

---

## 8. 当前状态与待办

**已完成且已验证**：Win7 适配、大屏重构（四态/预订）、收银历史订单+周月报表+CSV导出、双通道打印、退菜模块、强制释放、服务地址即时生效+二维码联动、多 IP 推荐、服务员 App 预订状态显示。

**潜在待办/风险**：
- Win7 实机最终验证依赖用户现场反馈（本机只能验证到 Electron 加载层）
- authMiddleware 是"局域网宽松模式"（无 token 放行），SaaS 化时必须重构为严格鉴权
- SaaS 化路线（用户有意向）：多租户 tenantId 改造 → 云部署+HTTPS → 商户/计费体系 → 小程序+支付 → 云打印+断网容灾。详见 `项目情况.md` 第七节

**用户沟通偏好**：用户非专业开发，需要"打包→放根目录→可安装"的交付方式；回答用中文；改完功能通常需要重新打包并更新根目录安装包。
